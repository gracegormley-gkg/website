---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - images/headshot.jpg
  - images/photo-01.jpg
  - images/photo-02.jpg
  - images/photo-03.jpg
  - images/photo-04.jpg
  - images/photo-05.jpg
  - images/photo-06.jpg
  - images/photo-07.jpg
  - images/photo-08.jpg
  - images/photo-09.jpg
  - images/photo-10.jpg
  - images/photo-11.jpg
  - images/photo-12.jpg
  - images/photo-13.jpg
autonomous: true
requirements:
  - PHOTO-04

must_haves:
  truths:
    - "The images/ directory exists at the repository root"
    - "Every file in images/ is ≤ 400 KB"
    - "Every file in images/ is ≤ 2000px on its longest edge"
    - "headshot.jpg exists in images/ — recompressed from the original, not resized"
    - "photo-01.jpg through photo-13.jpg all exist in images/"
    - "Original source files in the project root are untouched (images/ contains copies, not moved files)"
    - "The filenames in images/ exactly match the file paths in data.json photos array"
  artifacts:
    - path: "images/headshot.jpg"
      provides: "Optimized headshot for About page"
    - path: "images/photo-01.jpg"
      provides: "Optimized gallery photo 1 of 13"
    - path: "images/photo-13.jpg"
      provides: "Optimized gallery photo 13 of 13"
  key_links:
    - from: "data.json photos[].file"
      to: "images/photo-NN.jpg"
      via: "img src in gallery render script (Phase 4)"
      pattern: "images/photo-"
    - from: "data.json about.headshot"
      to: "images/headshot.jpg"
      via: "img src in About page (Phase 2)"
      pattern: "images/headshot.jpg"
---

<objective>
Optimize all 14 images (13 gallery photos + 1 headshot) from the project root to the images/ directory. This unblocks the Photography Gallery (Phase 4) and the About page headshot (Phase 2).

Purpose: Web-ready images are blocking — Phase 2 can't render the headshot and Phase 4 can't render the gallery without them. Process them once here at the correct spec.
Output: images/ directory with 14 JPEG files, all ≤ 400 KB and ≤ 2000px longest edge.
</objective>

<execution_context>
@/Users/gracegormley/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gracegormley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Photo naming convention defined by Plan 02 (data.json) -->
<!-- data.json photos array uses: images/photo-01.jpg through images/photo-13.jpg -->
<!-- data.json about.headshot uses: images/headshot.jpg -->
<!-- These paths are the API contract — image filenames in images/ MUST match exactly -->

Source files in project root → target files in images/:

  fave2.JPG                              → images/photo-01.jpg
  "IMG_0307 copy.JPG"                    → images/photo-02.jpg
  "IMG_0632 copy.JPG"                    → images/photo-03.jpg
  "IMG_8141 copy.JPG"                    → images/photo-04.jpg
  "IMG_8321 copy.JPG"                    → images/photo-05.jpg
  "IMG_8601 copy.JPG"                    → images/photo-06.jpg
  "IMG_8734 copy.JPG"                    → images/photo-07.jpg
  IMG_9609.JPG                           → images/photo-08.jpg
  "IMG_9697 copy.JPG"                    → images/photo-09.jpg
  "Lucy dive (also rly good).jpg"        → images/photo-10.jpg
  IMG_0720.jpg                           → images/photo-11.jpg
  IMG_0945.jpg                           → images/photo-12.jpg
  "IMG_8449-2 copy.jpg"                  → images/photo-13.jpg
  "GORMLEY, GRACE_SAMPLE_Jun2025_WEB_9 copy.jpg" → images/headshot.jpg
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Optimize all 14 images using sips and write to images/</name>
  <files>images/headshot.jpg, images/photo-01.jpg, images/photo-02.jpg, images/photo-03.jpg, images/photo-04.jpg, images/photo-05.jpg, images/photo-06.jpg, images/photo-07.jpg, images/photo-08.jpg, images/photo-09.jpg, images/photo-10.jpg, images/photo-11.jpg, images/photo-12.jpg, images/photo-13.jpg</files>
  <action>
All commands run from the project root: `/Users/gracegormley/Desktop/Grace's Website`

**Step 1: Create images/ directory**
```bash
mkdir -p "/Users/gracegormley/Desktop/Grace's Website/images"
```

**Step 2: Process the 9 large 5184px gallery photos (resize to 2000px, then recompress)**

Use the two-step sips process for each: resize first, then recompress at quality 70 if still over 400 KB. After resize from 5184px, expect ~600–900 KB; quality 70 should bring that to 200–400 KB.

```bash
cd "/Users/gracegormley/Desktop/Grace's Website"

# photo-01 (fave2.JPG — 5184x3456, 4.9 MB)
sips -Z 2000 "fave2.JPG" --out "images/photo-01.jpg"
sips -s format jpeg -s formatOptions 70 "images/photo-01.jpg" --out "images/photo-01.jpg"

# photo-02 (IMG_0307 copy.JPG — 5184x3456, 5.9 MB)
sips -Z 2000 "IMG_0307 copy.JPG" --out "images/photo-02.jpg"
sips -s format jpeg -s formatOptions 70 "images/photo-02.jpg" --out "images/photo-02.jpg"

# photo-03 (IMG_0632 copy.JPG — 5184x3456, 6.0 MB)
sips -Z 2000 "IMG_0632 copy.JPG" --out "images/photo-03.jpg"
sips -s format jpeg -s formatOptions 70 "images/photo-03.jpg" --out "images/photo-03.jpg"

# photo-04 (IMG_8141 copy.JPG — 5184x3456, 4.5 MB)
sips -Z 2000 "IMG_8141 copy.JPG" --out "images/photo-04.jpg"
sips -s format jpeg -s formatOptions 70 "images/photo-04.jpg" --out "images/photo-04.jpg"

# photo-05 (IMG_8321 copy.JPG — 5184x3456, 6.3 MB)
sips -Z 2000 "IMG_8321 copy.JPG" --out "images/photo-05.jpg"
sips -s format jpeg -s formatOptions 70 "images/photo-05.jpg" --out "images/photo-05.jpg"

# photo-06 (IMG_8601 copy.JPG — 5184x3456, 5.4 MB)
sips -Z 2000 "IMG_8601 copy.JPG" --out "images/photo-06.jpg"
sips -s format jpeg -s formatOptions 70 "images/photo-06.jpg" --out "images/photo-06.jpg"

# photo-07 (IMG_8734 copy.JPG — 5184x3456, 5.1 MB)
sips -Z 2000 "IMG_8734 copy.JPG" --out "images/photo-07.jpg"
sips -s format jpeg -s formatOptions 70 "images/photo-07.jpg" --out "images/photo-07.jpg"

# photo-08 (IMG_9609.JPG — 5184x3456, 5.5 MB)
sips -Z 2000 "IMG_9609.JPG" --out "images/photo-08.jpg"
sips -s format jpeg -s formatOptions 70 "images/photo-08.jpg" --out "images/photo-08.jpg"

# photo-09 (IMG_9697 copy.JPG — 5184x3456, 5.9 MB)
sips -Z 2000 "IMG_9697 copy.JPG" --out "images/photo-09.jpg"
sips -s format jpeg -s formatOptions 70 "images/photo-09.jpg" --out "images/photo-09.jpg"

# photo-10 (Lucy dive (also rly good).jpg — 6000x4000, 6.7 MB)
sips -Z 2000 "Lucy dive (also rly good).jpg" --out "images/photo-10.jpg"
sips -s format jpeg -s formatOptions 70 "images/photo-10.jpg" --out "images/photo-10.jpg"
```

**Step 3: Process the 3 already-small gallery photos (2048px — recompress only if over 400 KB)**

For these, skip the resize step (already ≤ 2048px). Recompress to quality 70 to target ≤ 400 KB.

```bash
# photo-11 (IMG_0720.jpg — 2048x1365, 603 KB)
sips -s format jpeg -s formatOptions 70 "IMG_0720.jpg" --out "images/photo-11.jpg"

# photo-12 (IMG_0945.jpg — 2048x1365, 1.1 MB)
sips -s format jpeg -s formatOptions 70 "IMG_0945.jpg" --out "images/photo-12.jpg"

# photo-13 (IMG_8449-2 copy.jpg — 2048x1365, 996 KB)
sips -s format jpeg -s formatOptions 70 "IMG_8449-2 copy.jpg" --out "images/photo-13.jpg"
```

**Step 4: Process the headshot (1600px — recompress only, higher quality)**

The headshot is not a gallery photo. Use quality 75 (slightly higher than gallery) to preserve portrait quality.

```bash
# headshot (GORMLEY, GRACE_SAMPLE_Jun2025_WEB_9 copy.jpg — 1600x1200, 834 KB)
sips -s format jpeg -s formatOptions 75 "GORMLEY, GRACE_SAMPLE_Jun2025_WEB_9 copy.jpg" --out "images/headshot.jpg"
```

**After each file, check size and dimensions:**
```bash
ls -lh images/*.jpg
sips -g pixelWidth -g pixelHeight images/*.jpg
```

**Quality floor:** Do not go below quality 65 for any photo. If a file is still over 400 KB at quality 70, try quality 65. If still over 400 KB at quality 65, flag it in the summary and leave at quality 65 — a slightly over-spec photo is better than a visibly degraded one.

**Do NOT overwrite the originals in the project root.** The originals are the master copies.
  </action>
  <verify>
    <automated>python3 -c "
import subprocess, os, sys

images_dir = \"/Users/gracegormley/Desktop/Grace's Website/images\"

expected = ['headshot.jpg'] + [f'photo-{str(i).zfill(2)}.jpg' for i in range(1, 14)]
missing = [f for f in expected if not os.path.exists(os.path.join(images_dir, f))]
if missing:
    print('MISSING FILES:', missing)
    sys.exit(1)

oversized = []
for fname in expected:
    fpath = os.path.join(images_dir, fname)
    size_kb = os.path.getsize(fpath) / 1024
    if size_kb > 400:
        oversized.append(f'{fname}: {size_kb:.0f} KB')

if oversized:
    print('FILES OVER 400 KB:', oversized)
    sys.exit(1)

print(f'PASS: all {len(expected)} files present and within size budget')

# Check dimensions via sips
result = subprocess.run(
    ['sips', '-g', 'pixelWidth', '-g', 'pixelHeight'] + [os.path.join(images_dir, f) for f in expected],
    capture_output=True, text=True
)
lines = result.stdout.split('\\n')
oversized_dim = []
fname = None
w = h = None
for line in lines:
    line = line.strip()
    if line.endswith('.jpg'):
        fname = os.path.basename(line)
    elif 'pixelWidth' in line:
        w = int(line.split(':')[1].strip())
    elif 'pixelHeight' in line:
        h = int(line.split(':')[1].strip())
        if fname and w and h and max(w, h) > 2000:
            oversized_dim.append(f'{fname}: {w}x{h}')
        fname = w = h = None

if oversized_dim:
    print('FILES OVER 2000px:', oversized_dim)
    sys.exit(1)

print('PASS: all images within 2000px dimension limit')
"</automated>
  </verify>
  <done>
- images/ directory exists with 14 files: headshot.jpg and photo-01.jpg through photo-13.jpg
- Every file is ≤ 400 KB (or flagged in summary if quality floor of 65 was reached)
- Every file is ≤ 2000px on its longest edge
- Source files in the project root are untouched
- File names in images/ match data.json photo file paths exactly
  </done>
</task>

</tasks>

<verification>
After task completion:

1. Run the automated verify command above — it must exit 0.
2. Run: `ls -lh "/Users/gracegormley/Desktop/Grace's Website/images/"` — confirm 14 files, all reasonably sized.
3. Run: `sips -g pixelWidth -g pixelHeight "/Users/gracegormley/Desktop/Grace's Website/images/"*.jpg` — confirm all longest edges ≤ 2000px.
4. Open 2–3 optimized images in Preview to confirm no visible blocking artifacts — photo quality should look clean.
5. Confirm source originals still exist in the project root (not moved or deleted).
</verification>

<success_criteria>
- 14 files in images/: headshot.jpg + photo-01.jpg through photo-13.jpg
- All 14 files ≤ 400 KB (or noted at quality floor 65 in summary)
- All 14 files ≤ 2000px longest edge
- No visual artifacting visible at normal viewing size
- Original source files in project root untouched
- images/ committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` documenting:
- Final file inventory: each filename, dimensions, and file size in KB
- Any files that hit the quality 65 floor (flag these for Phase 4 curation review)
- Confirmation that source originals are untouched
- Source → target filename mapping (the 14 mappings listed in the interfaces block above)
</output>
