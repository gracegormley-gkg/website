---
phase: 02-shell-and-about
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - index.html
  - assets/resume.pdf
autonomous: true
requirements: [ABOUT-01, ABOUT-02, ABOUT-03]

must_haves:
  truths:
    - "Visiting index.html shows Grace's headshot, full name in an h1, and bio paragraph"
    - "The tagline names both journalism and data science in a distinctive, non-generic way"
    - "Clicking 'Download Resume' downloads a PDF named Grace-Gormley-Resume.pdf to the visitor's device"
    - "About nav link is highlighted when on index.html"
    - "Photo credit for the headshot is visible"
  artifacts:
    - path: "index.html"
      provides: "About page — static HTML with headshot, name, tagline, bio, resume download button"
      contains: "Grace Gormley"
    - path: "assets/resume.pdf"
      provides: "Resume PDF served at same origin for download attribute to work"
      min_size_kb: 50
  key_links:
    - from: "index.html <a href='assets/resume.pdf' download>"
      to: "assets/resume.pdf"
      via: "HTML download attribute (same-origin)"
      pattern: "download=\"Grace-Gormley-Resume\\.pdf\""
    - from: "index.html <img src='images/headshot.jpg'>"
      to: "images/headshot.jpg"
      via: "relative path (Phase 1 delivered this file)"
      pattern: "images/headshot\\.jpg"
    - from: "index.html"
      to: "components.js"
      via: "<script type=\"module\" src=\"components.js\">"
      pattern: "type=\"module\""
---

<objective>
Build index.html (the About page) as static HTML and create the assets/ directory with the resume PDF.

Purpose: index.html is the site's entry point — it is the page GitHub Pages serves at the root URL and the first thing editors and recruiters see. Static HTML (not a fetch-from-JSON approach) is used because: it renders instantly with no async delay, works with JavaScript disabled, and the About content is stable. The footer contact links are handled by the SiteFooter web component from Plan 01 — they auto-update if contact info changes.

Output: index.html with a complete About layout (headshot, name, tagline, bio, resume download), and assets/resume.pdf copied from the project root.
</objective>

<execution_context>
@/Users/gracegormley/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gracegormley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shell-and-about/02-01-SUMMARY.md

<interfaces>
<!-- Content values — embed these directly in static HTML, do not fetch from data.json at runtime -->

From data.json about object:
- name: "Grace Gormley"
- tagline: "Journalism and data science student at Northwestern University Medill"
  NOTE: This tagline is generic and MUST be replaced. See Task 1 action for the required stronger version.
- bio: "I am a journalism and data science double major in my sophomore year at Northwestern University, in the Medill School of Journalism. I strive to combine my data science skills with people-centric reporting to hold those in power accountable and tell important stories. I get excited about investigative and watchdog journalism driven by data."
- headshot: "images/headshot.jpg"
- headshot_credit: "Anna Hoch-Kenney"
- resume_pdf: "assets/resume.pdf"

Resume source file (project root):
- "Resume - Grace Gormley (Jan 2026) copy.pdf"

From style.css (Phase 1 tokens — use in layout classes, do not redefine):
- --max-width-content: 720px  (readable column for bio)
- --space-8: 2rem
- --space-12: 3rem
- --color-ink-muted: #555555
- --text-xs: 0.75rem
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create index.html — About page with headshot, tagline, bio, and resume download</name>
  <files>index.html</files>
  <action>
Create `/Users/gracegormley/Desktop/Grace's Website/index.html` as a complete static About page.

**Tagline requirement (ABOUT-03):** The data.json tagline ("Journalism and data science student at Northwestern University Medill") is too generic — it describes a category, not Grace specifically. Replace it with: "Reporting with data. Writing with purpose." — this names both angles distinctively and reads like an editorial byline, not a form field. Embed this directly in the HTML.

**Page structure:**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>About — Grace Gormley</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Newsreader:ital,opsz,wght@0,6..72,300..800;1,6..72,300..800&family=Inter:opsz,wght@14..32,400;14..32,500;14..32,600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="style.css">
  <script type="module" src="components.js"></script>
</head>
<body>
  <site-nav></site-nav>
  <main class="about-page">
    <section class="about-hero">
      <img class="about-headshot"
           src="images/headshot.jpg"
           alt="Grace Gormley, photojournalist and data journalism student at Northwestern Medill"
           width="320" height="240">
      <div class="about-hero__text">
        <h1>Grace Gormley</h1>
        <p class="about-tagline">Reporting with data. Writing with purpose.</p>
      </div>
    </section>
    <section class="about-bio">
      <p>I am a journalism and data science double major in my sophomore year at Northwestern University, in the Medill School of Journalism. I strive to combine my data science skills with people-centric reporting to hold those in power accountable and tell important stories. I get excited about investigative and watchdog journalism driven by data.</p>
    </section>
    <section class="about-actions">
      <a href="assets/resume.pdf" download="Grace-Gormley-Resume.pdf" class="btn-download">Download Resume</a>
    </section>
    <p class="about-credit">Headshot: Anna Hoch-Kenney</p>
  </main>
  <site-footer></site-footer>
</body>
</html>
```

**CSS to append to style.css for About page layout:**

Append these rules to the bottom of style.css (after the nav/footer rules from Plan 01):

```css
/* About page — Phase 2 */
.about-page {
  max-width: var(--max-width-content);
  margin: var(--space-12) auto;
  padding: 0 var(--space-8);
}

.about-hero {
  display: flex;
  gap: var(--space-8);
  align-items: flex-start;
  margin-bottom: var(--space-12);
}

.about-headshot {
  width: 200px;
  height: auto;
  border-radius: var(--border-radius);
  flex-shrink: 0;
}

.about-hero__text h1 {
  font-size: var(--text-2xl);
  margin-bottom: var(--space-2);
}

.about-tagline {
  font-family: var(--font-editorial);
  font-size: var(--text-lg);
  color: var(--color-ink-muted);
  font-style: italic;
}

.about-bio {
  margin-bottom: var(--space-8);
  line-height: var(--leading-body);
}

.about-actions {
  margin-bottom: var(--space-4);
}

.btn-download {
  display: inline-block;
  padding: var(--space-3) var(--space-6);
  background: var(--color-accent);
  color: #ffffff;
  text-decoration: none;
  border-radius: var(--border-radius);
  font-family: var(--font-ui);
  font-size: var(--text-sm);
  font-weight: 600;
  transition: background var(--transition-fast);
}

.btn-download:hover {
  background: var(--color-link-hover);
  color: #ffffff;
}

.about-credit {
  font-size: var(--text-xs);
  color: var(--color-ink-muted);
  margin-top: var(--space-4);
}
```

Note: Also modify style.css to append these About-page rules. Files modified = index.html AND style.css (update the files_modified to include style.css if the executor tool requires it — the CSS additions belong in this task since they are tightly coupled to index.html layout).
  </action>
  <verify>
    <automated>
      node -e "
const fs=require('fs');
const html=fs.readFileSync('/Users/gracegormley/Desktop/Grace\'s Website/index.html','utf8');
const checks=[
  'Grace Gormley',
  'Reporting with data. Writing with purpose.',
  'images/headshot.jpg',
  'assets/resume.pdf',
  'download=\"Grace-Gormley-Resume.pdf\"',
  'Anna Hoch-Kenney',
  'site-nav',
  'site-footer',
  'type=\"module\"',
  'viewport'
];
const missing=checks.filter(c=>!html.includes(c));
if(missing.length){console.error('index.html MISSING:',missing);process.exit(1)}
else{console.log('index.html OK')}
"
    </automated>
  </verify>
  <done>
    - index.html exists at repository root
    - Contains Grace Gormley in h1
    - Tagline reads "Reporting with data. Writing with purpose." (not the generic data.json value)
    - Headshot img points to images/headshot.jpg
    - Resume download link: href="assets/resume.pdf" download="Grace-Gormley-Resume.pdf"
    - Headshot credit for Anna Hoch-Kenney is present
    - site-nav and site-footer custom elements present
    - type="module" on script tag
    - viewport meta present
    - style.css has .about-page, .about-hero, .btn-download rules appended
  </done>
</task>

<task type="auto">
  <name>Task 2: Create assets/ directory and copy resume PDF</name>
  <files>assets/resume.pdf</files>
  <action>
Create the `assets/` directory and copy the resume PDF so the download link in index.html resolves.

Run these commands from the project root:
```bash
mkdir -p "/Users/gracegormley/Desktop/Grace's Website/assets"
cp "/Users/gracegormley/Desktop/Grace's Website/Resume - Grace Gormley (Jan 2026) copy.pdf" \
   "/Users/gracegormley/Desktop/Grace's Website/assets/resume.pdf"
```

Verify the copy succeeded and the file is non-empty (should be several hundred KB — it is a real resume PDF).

The `data.json` field `about.resume_pdf` already points to `"assets/resume.pdf"` — no data.json changes needed. The download attribute in index.html suggests the filename `Grace-Gormley-Resume.pdf` to the browser — the actual file on disk stays as `resume.pdf`.

The `download` attribute works on same-origin links. Since GitHub Pages serves everything from the same origin (`gracegormley-gkg.github.io/EIS-Final`), this works without restriction.
  </action>
  <verify>
    <automated>
      node -e "
const fs=require('fs');
const stats=fs.statSync('/Users/gracegormley/Desktop/Grace\'s Website/assets/resume.pdf');
if(stats.size < 10000){console.error('assets/resume.pdf too small — copy may have failed, size:',stats.size);process.exit(1)}
else{console.log('assets/resume.pdf exists, size:',Math.round(stats.size/1024)+'KB')}
"
    </automated>
  </verify>
  <done>
    - assets/ directory exists
    - assets/resume.pdf exists and is at least 10 KB (real PDF, not an empty or corrupt file)
    - Source file in project root is unchanged
  </done>
</task>

</tasks>

<verification>
Start local server and verify the complete About page:
```bash
cd "/Users/gracegormley/Desktop/Grace's Website" && python3 -m http.server 8080
```

Open http://localhost:8080 and confirm:
1. Headshot renders (not a broken image icon)
2. Name "Grace Gormley" appears as the page heading
3. Tagline reads "Reporting with data. Writing with purpose." — not the generic student bio line
4. Bio paragraph is present
5. "Download Resume" button is styled and visible
6. Clicking Download Resume downloads a PDF (browser download bar appears); open it and confirm it is Grace's resume
7. "About" nav link is highlighted (test both http://localhost:8080 and http://localhost:8080/index.html)
8. Footer shows gracegormley@gmail.com and LinkedIn links
</verification>

<success_criteria>
- index.html renders the complete About page: headshot, name (h1), distinctive tagline, bio, resume download button, headshot credit
- ABOUT-03 satisfied: tagline "Reporting with data. Writing with purpose." names both journalism and data science angles distinctively
- assets/resume.pdf exists and downloads successfully when the button is clicked
- Active nav state correctly highlights "About" link on the home URL
</success_criteria>

<output>
After completion, create `.planning/phases/02-shell-and-about/02-02-SUMMARY.md` following the summary template.
</output>
