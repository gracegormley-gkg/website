---
phase: 02-shell-and-about
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified: []
autonomous: false
requirements: [SITE-04]

user_setup:
  - service: github-pages
    why: "GitHub Pages configuration requires clicking through repository Settings UI — cannot be done via CLI without a GitHub token"
    env_vars: []
    dashboard_config:
      - task: "Enable GitHub Pages on the repository"
        location: "Repository on github.com → Settings → Pages → Source: Deploy from a branch → Branch: main → Folder: / (root) → Save"

must_haves:
  truths:
    - "Visiting https://gracegormley-gkg.github.io/EIS-Final/ returns the About page within 3 seconds"
    - "The deployed page shows Grace's headshot, name, and bio"
    - "All 7 nav links resolve on the live site (no 404s)"
    - "The resume download works on the live site"
  artifacts:
    - path: ".git/config"
      provides: "Remote origin pointing to https://github.com/gracegormley-gkg/EIS-Final"
      contains: "gracegormley-gkg/EIS-Final"
  key_links:
    - from: "local main branch"
      to: "github.com/gracegormley-gkg/EIS-Final"
      via: "git push -u origin main"
      pattern: "origin.*main"
    - from: "GitHub Pages"
      to: "index.html at repo root"
      via: "branch: main, folder: / (root)"
      pattern: "github\\.io"
---

<objective>
Set up the GitHub remote, rename the local branch from master to main, push all work from Plans 01 and 02, and configure GitHub Pages so the site is publicly accessible.

Purpose: Phase 2's final success criterion is that the site is live and accessible. Plans 01 and 02 built all the files locally. This plan gets them onto GitHub and turns on the Pages hosting. After this plan, editors and recruiters can visit the live URL.

Output: GitHub repository at https://github.com/gracegormley-gkg/EIS-Final has all committed files on the main branch, GitHub Pages is configured to deploy from main, and the site is live at https://gracegormley-gkg.github.io/EIS-Final/.
</objective>

<execution_context>
@/Users/gracegormley/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gracegormley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shell-and-about/02-01-SUMMARY.md
@.planning/phases/02-shell-and-about/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename branch to main, add GitHub remote, and push</name>
  <files></files>
  <action>
The local repository is on the `master` branch. GitHub Pages defaults to `main`. Rename the branch and push to the GitHub remote.

Run these commands in sequence from the project root `/Users/gracegormley/Desktop/Grace's Website`:

```bash
# Step 1: Rename local branch master → main
git branch -m master main

# Step 2: Add the GitHub remote
git remote add origin https://github.com/gracegormley-gkg/EIS-Final.git

# Step 3: Verify remote is set correctly
git remote -v

# Step 4: Push main branch and set upstream tracking
git push -u origin main
```

If git push prompts for GitHub credentials, authenticate using a Personal Access Token (PAT) as the password. GitHub deprecated password authentication in 2021 — only PATs or SSH keys work. If authentication fails, surface a checkpoint for the user.

After a successful push, confirm the repository at https://github.com/gracegormley-gkg/EIS-Final shows all files (index.html, components.js, style.css, data.json, images/, assets/).
  </action>
  <verify>
    <automated>cd "/Users/gracegormley/Desktop/Grace's Website" &amp;&amp; git remote get-url origin &amp;&amp; git branch --show-current</automated>
  </verify>
  <done>
    - Local branch is named `main` (not `master`)
    - git remote `origin` points to https://github.com/gracegormley-gkg/EIS-Final.git
    - git push succeeded — all commits from Phases 1 and 2 are on the remote main branch
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Enable GitHub Pages and verify live site</name>
  <action>
All Phase 2 files (components.js, 6 stub pages, index.html, assets/resume.pdf, style.css with nav/footer/about CSS) are pushed to https://github.com/gracegormley-gkg/EIS-Final on the main branch.

GitHub Pages must be enabled manually in the repository Settings UI (cannot be done via CLI without a GitHub token).

**Step 1 — Enable GitHub Pages:**
1. Go to https://github.com/gracegormley-gkg/EIS-Final
2. Click "Settings" tab
3. Click "Pages" in the left sidebar
4. Under "Source", select: "Deploy from a branch"
5. Under "Branch", select: `main` and folder `/ (root)`
6. Click "Save"
7. Wait ~10 minutes for the first deploy (subsequent deploys take ~60 seconds)
8. GitHub will show a banner: "Your site is live at https://gracegormley-gkg.github.io/EIS-Final/"

**Step 2 — Verify the live site:**
1. Visit https://gracegormley-gkg.github.io/EIS-Final/
2. Confirm the About page loads within 3 seconds with Grace's headshot, name, and bio visible
3. Click each of the 7 nav links — confirm no 404 errors
4. Confirm the "About" nav link is highlighted on the home URL
5. Click "Download Resume" — confirm the PDF downloads
6. Click the LinkedIn link in the footer — confirm it opens https://www.linkedin.com/in/grace-gormley/
7. Click the email link in the footer — confirm it opens your email client with gracegormley@gmail.com pre-filled
  </action>
  <verify>
    <automated>MISSING — live deployment requires human verification of the published URL</automated>
  </verify>
  <done>
    - GitHub Pages is enabled on the repository
    - https://gracegormley-gkg.github.io/EIS-Final/ loads the About page within 3 seconds
    - All 7 nav links resolve on the live site
    - Resume download works on the live domain
    - Footer email and LinkedIn links work
  </done>
  <resume-signal>Type "approved" when the live site passes all 7 checks, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Manual verification on the live GitHub Pages URL (https://gracegormley-gkg.github.io/EIS-Final/):

Phase 2 success criteria checklist:
- [ ] Page loads within 3 seconds — headshot, name, bio all visible
- [ ] All 7 nav links resolve (no 404s on clips, photos, video, knight-lab, suso-camp, awards)
- [ ] Active state: "About" is highlighted when visiting the home URL
- [ ] Email link opens email client with gracegormley@gmail.com
- [ ] LinkedIn link opens https://www.linkedin.com/in/grace-gormley/ in a new tab
- [ ] "Download Resume" downloads Grace-Gormley-Resume.pdf (not a 404)
- [ ] Tagline reads "Reporting with data. Writing with purpose." — not the generic student bio
</verification>

<success_criteria>
- Site is publicly accessible at https://gracegormley-gkg.github.io/EIS-Final/
- About page shows headshot, name (h1), distinctive tagline, bio paragraph
- All 7 nav links resolve to real pages (no 404s)
- Resume download works on the live domain
- SITE-04 complete: site is deployed and publicly accessible via GitHub Pages
</success_criteria>

<output>
After completion, create `.planning/phases/02-shell-and-about/02-03-SUMMARY.md` following the summary template.
</output>
