---
phase: 03-clips-and-awards
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - awards.html
autonomous: true
requirements:
  - AWARD-01
  - AWARD-02
must_haves:
  truths:
    - "Awards page has two clearly distinct sections: 'Awards' and 'Press Coverage' — a visitor can tell them apart at a glance"
    - "All 11 award entries render with award name, organization, and year; entries without a live URL use pdf_fallback; entries with no link at all render without an anchor"
    - "Press Coverage section shows all 3 items (Verde Magazine, Hearts of Gold Podcast, Paly Voice) with working links"
    - "No award entry produces a broken <a href=''>  that navigates to the current page"
  artifacts:
    - path: "awards.html"
      provides: "Awards page with two sections rendered from data.json awards and press_coverage arrays"
      contains: "awards-list"
  key_links:
    - from: "awards.html inline <script>"
      to: "data.json awards array (11 entries)"
      via: "fetch('./data.json').then(r => r.json()).then(data => data.awards)"
      pattern: "fetch.*data\\.json"
    - from: "awards.html inline <script>"
      to: "data.json press_coverage array (3 entries)"
      via: "data.press_coverage"
      pattern: "press_coverage"
    - from: "award render template"
      to: "graceful URL fallback logic"
      via: "const href = a.url || a.pdf_fallback"
      pattern: "a\\.url.*a\\.pdf_fallback"
---

<objective>
Build awards.html with two fetch-and-render sections: award-winning articles and press coverage. Awards data and press coverage data are already complete in data.json — this plan is pure rendering.

Purpose: The Awards page shows Grace's recognition and external press. Editors scan it to confirm credibility and reach.
Output: awards.html — fully rendered awards list and press coverage section, both from data.json.
</objective>

<execution_context>
@/Users/gracegormley/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gracegormley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-clips-and-awards/03-RESEARCH.md
@.planning/phases/03-clips-and-awards/03-01-SUMMARY.md

<interfaces>
<!-- Key data contracts from data.json. Executor should use these directly — no codebase exploration needed. -->

From data.json — awards array (11 entries). URL handling summary:
- Entries with url present: use url as href
- Entries with url="" but pdf_fallback present ("Beyond the Game", "Breaking the Mold"): use pdf_fallback as href
- Entries with url="" AND pdf_fallback="" ("Excellence Award", "Casey Nichols Award"): render without any link

Entry schema:
```json
{
  "name": "Award name string",
  "organization": "Granting organization string",
  "year": "YYYY",
  "article_title": "Article or piece title, or empty string",
  "url": "https://... or empty string",
  "pdf_fallback": "https://... or empty string"
}
```

From data.json — press_coverage array (3 fully-populated entries):
```json
{
  "outlet": "Publication/podcast name",
  "title": "Article/episode title",
  "url": "https://...",
  "description": "One sentence description"
}
```

From style.css (added in Plan 01 — already present when this plan executes):
- .page-content, .awards-section, .press-section, .section-heading
- .award-entry, .award-name, .award-org, .award-piece
- .press-item, .press-outlet, .press-title, .press-desc

From awards.html (current stub — replace body content):
```html
<body>
  <site-nav></site-nav>
  <main class="page-content">
    <h1>Awards</h1>
    <p>Content coming soon.</p>
  </main>
  <site-footer></site-footer>
</body>
```
Note: <head> boilerplate is correct — keep it unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build awards.html with awards and press coverage sections</name>
  <files>awards.html</files>
  <action>
    Replace the awards.html body content. Keep the <head> entirely unchanged.

    Replace everything inside <body>:

    ```html
    <body>
      <site-nav></site-nav>
      <main class="page-content">
        <h1>Awards &amp; Press</h1>
        <section class="awards-section">
          <h2 class="section-heading">Awards</h2>
          <div id="awards-list">
            <p>Loading...</p>
          </div>
        </section>
        <section class="press-section">
          <h2 class="section-heading">Press Coverage</h2>
          <div id="press-list">
            <p>Loading...</p>
          </div>
        </section>
      </main>
      <site-footer></site-footer>
      <script>
        fetch('./data.json')
          .then(r => r.json())
          .then(data => {
            // Awards section
            const awardsContainer = document.getElementById('awards-list');
            awardsContainer.innerHTML = data.awards.map(a => {
              const href = a.url || a.pdf_fallback;
              const articleLine = a.article_title
                ? `<p class="award-piece">${
                    href
                      ? `<a href="${href}" target="_blank" rel="noopener">${a.article_title}</a>`
                      : a.article_title
                  }</p>`
                : '';
              return `
                <div class="award-entry">
                  <h3 class="award-name">${a.name}</h3>
                  <p class="award-org">${a.organization} &mdash; ${a.year}</p>
                  ${articleLine}
                </div>
              `;
            }).join('');

            // Press coverage section
            const pressContainer = document.getElementById('press-list');
            pressContainer.innerHTML = data.press_coverage.map(item => `
              <div class="press-item">
                <p class="press-outlet">${item.outlet}</p>
                <h3 class="press-title">
                  <a href="${item.url}" target="_blank" rel="noopener">${item.title}</a>
                </h3>
                <p class="press-desc">${item.description}</p>
              </div>
            `).join('');
          })
          .catch(() => {
            document.getElementById('awards-list').innerHTML =
              '<p>Unable to load awards. Please try again.</p>';
            document.getElementById('press-list').innerHTML =
              '<p>Unable to load press coverage. Please try again.</p>';
          });
      </script>
    </body>
    ```

    CRITICAL URL HANDLING — implement exactly:
    - const href = a.url || a.pdf_fallback  (empty string is falsy — this is intentional)
    - Only wrap article_title in <a> if href is truthy
    - If article_title is empty string, skip the article line entirely
    - Result: "Excellence Award in Feature Writing" and "Casey Nichols Service Above Self Award" render as award name + organization + year only — no article line

    CRITICAL: All external links must have target="_blank" rel="noopener"
    CRITICAL: Use h3 for award-name (not h2) — the section heading "Awards" is h2; award names are h3 to maintain heading hierarchy

    DO NOT touch style.css — all CSS classes were added in Plan 01.
  </action>
  <verify>
    <automated>python3 -c "
import re, sys
html = open('awards.html').read()
assert 'awards-list' in html, 'Missing awards-list container'
assert 'press-list' in html, 'Missing press-list container'
assert 'a.url || a.pdf_fallback' in html, 'Missing graceful URL fallback logic'
assert 'rel=\"noopener\"' in html, 'Missing rel=noopener on external links'
assert 'press_coverage' in html, 'Missing press_coverage reference'
print('OK: awards.html structure verified')
"</automated>
  </verify>
  <done>
    - awards.html has #awards-list and #press-list container divs
    - Graceful URL fallback logic present: const href = a.url || a.pdf_fallback
    - All external links have target="_blank" rel="noopener"
    - Page renders 11 awards and 3 press coverage items when viewed via local server
    - Awards without a linked piece (Excellence Award, Casey Nichols) show no article line
  </done>
</task>

</tasks>

<verification>
Start local server: python3 -m http.server 8080 (from project root)
Open: http://localhost:8080/awards.html

Check:
1. Page title shows "Awards — Grace Gormley" in browser tab
2. Nav is present and "Awards" link is highlighted as active
3. Two distinct sections visible: "Awards" heading and "Press Coverage" heading
4. Awards section shows 11 entries — each with award name, organization, and year
5. "Beyond the Game" and "Breaking the Mold" show article title as a link (using PDF fallback URL)
6. "Excellence Award in Feature Writing" shows award name + organization only — no article line
7. "Casey Nichols Service Above Self Award" shows award name + organization only — no article line
8. Press Coverage section shows Verde Magazine, Hearts of Gold Podcast, and Paly Voice entries
9. No JavaScript errors in DevTools console
10. No <a href=""> dead links (clicking any award link navigates away from the page, not to itself)
</verification>

<success_criteria>
- awards.html renders 11 awards from data.json with correct URL fallback logic
- Awards and Press Coverage are visually distinct sections with separate headings
- All 3 press coverage items visible with working external links
- No entries with empty url AND empty pdf_fallback produce an anchor tag
- No JavaScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-clips-and-awards/03-02-SUMMARY.md`
</output>
