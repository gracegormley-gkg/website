---
phase: 04-photography-gallery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data.json
  - photos.html
  - style.css
autonomous: false
requirements:
  - PHOTO-01
  - PHOTO-02
  - PHOTO-03

must_haves:
  truths:
    - "data.json photos array contains zero PLACEHOLDER strings for caption or alt fields"
    - "photos.html renders a CSS Grid of 13 clickable thumbnails fetched from data.json"
    - "Clicking any thumbnail opens a full-size image in a <dialog> lightbox with caption"
    - "Lightbox responds to ArrowRight/ArrowLeft (navigate), Escape (close), and touch swipe left/right"
    - "Gallery uses --max-width-wide (1100px) container, not the 720px content column"
  artifacts:
    - path: "data.json"
      provides: "13 photo entries with real caption and alt text (no PLACEHOLDER)"
      contains: "\"caption\": \"[non-placeholder content]\""
    - path: "photos.html"
      provides: "Full photography gallery page with grid + lightbox"
      min_lines: 80
    - path: "style.css"
      provides: "Gallery CSS (.photo-page, .photo-grid, .photo-thumb, .photo-lightbox and related)"
      contains: ".photo-grid"
  key_links:
    - from: "photos.html inline <script>"
      to: "./data.json"
      via: "fetch('./data.json').then(r => r.json())"
      pattern: "fetch\\('./data\\.json'\\)"
    - from: ".photo-thumb button click handler"
      to: "dialog#lightbox"
      via: "openLightbox(index) calls dialog.showModal()"
      pattern: "showModal"
    - from: "dialog keydown listener"
      to: "navigate() / dialog.close()"
      via: "ArrowRight, ArrowLeft, Escape keys"
      pattern: "keydown"
---

<objective>
Populate all 13 photo captions and alt text in data.json, then build the complete photography gallery page: a CSS Grid thumbnail grid and a native <dialog> lightbox with keyboard/touch navigation.

Purpose: Delivers all three photography requirements (PHOTO-01, PHOTO-02, PHOTO-03) in one integrated plan. The caption population must precede rendering so PHOTO-03 is never in a shipped-but-incomplete state.

Output: Updated data.json (no PLACEHOLDER in photos), complete photos.html, and gallery CSS additions to style.css.
</objective>

<execution_context>
@/Users/gracegormley/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gracegormley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gracegormley/Desktop/Grace's Website/.planning/PROJECT.md
@/Users/gracegormley/Desktop/Grace's Website/.planning/ROADMAP.md
@/Users/gracegormley/Desktop/Grace's Website/.planning/STATE.md
@/Users/gracegormley/Desktop/Grace's Website/data.json
@/Users/gracegormley/Desktop/Grace's Website/photos.html
@/Users/gracegormley/Desktop/Grace's Website/style.css
@/Users/gracegormley/Desktop/Grace's Website/clips.html
@/Users/gracegormley/Desktop/Grace's Website/grace-gormley-portfolio-reference.md

<interfaces>
<!-- Key contracts the executor needs. Do not re-explore the codebase for these. -->

From data.json — photos array shape (all 13 entries currently have PLACEHOLDER caption and alt):
```json
{
  "file": "images/photo-01.jpg",
  "caption": "PLACEHOLDER — one sentence of story context",
  "credit": "Grace Gormley",
  "alt": "PLACEHOLDER — accessible description"
}
```
The `credit` field ("Grace Gormley") is already correct on all 13 entries — do NOT modify it.
Only `caption` and `alt` need replacing on all 13 entries.

From style.css — available CSS custom properties (use these; do not invent new tokens):
```css
--font-editorial: 'Newsreader', Georgia, serif;
--font-ui: 'Inter', system-ui, sans-serif;
--text-xs: 0.75rem;    /* captions, credits */
--text-sm: 0.875rem;   /* metadata */
--text-base: 1rem;
--text-lg: 1.125rem;
--text-xl: 1.5rem;
--text-2xl: 2rem;
--color-ink: #1a1a1a;
--color-ink-muted: #555555;
--color-border: #e0e0e0;
--color-bg: #ffffff;
--color-bg-subtle: #f9f8f6;
--color-accent: #1a3a5c;
--space-1 through --space-24 (8px grid)
--max-width-content: 720px;   /* DO NOT use for gallery */
--max-width-wide: 1100px;     /* USE THIS for .photo-page */
--border-radius: 4px;
--transition-fast: 150ms ease;
```

From clips.html — established fetch-and-render pattern (copy this pattern exactly):
```javascript
fetch('./data.json')
  .then(r => r.json())
  .then(data => {
    // render from data array
  })
  .catch(() => {
    // show error message
  });
```

From grace-gormley-portfolio-reference.md — original Wix filenames for caption context:
Original 10 Wix photos: Henry Fly.jpg, IMG_8141.JPG, fave2.JPG, Julian's backkk.jpg,
IMG_8601.JPG, IMG_8321.JPG, IMG_0945.JPG, (unnamed x2), IMG_0720.JPG
Project images in repo: photo-01.jpg through photo-13.jpg (13 files)
The filenames from the untracked git status include: IMG_0307 copy.JPG, IMG_0632 copy.JPG,
IMG_0720.jpg, IMG_0945.jpg, IMG_8141 copy.JPG, IMG_8321 copy.JPG, IMG_8449-2 copy.jpg,
IMG_8601 copy.JPG, IMG_8734 copy.JPG, IMG_9609.JPG, IMG_9697 copy.JPG,
Lucy dive (also rly good).jpg, fave2.JPG, GORMLEY, GRACE_SAMPLE_Jun2025_WEB_9 copy.jpg
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Provide captions and alt text for all 13 photos</name>
  <what-built>data.json currently has 13 photo entries with PLACEHOLDER caption and alt text. Real captions are needed to satisfy PHOTO-03 — the requirement that each photo has story context, not a filename.</what-built>
  <how-to-verify>
    Open data.json. For each of the 13 photos (photo-01.jpg through photo-13.jpg), provide:
    - `caption`: One sentence of journalistic story context describing what is happening in the photo and why it matters. Examples: "Swimmers compete at the 2024 CCS Championships in Santa Clara." or "A Buddhist monk leads chanting at the Vaisakha festival in downtown Palo Alto, August 2025."
    - `alt`: An accessible description of the photo for screen readers. Example: "A swimmer in a blue cap mid-stroke in a crowded pool lane."

    You can view the images by opening them from the images/ folder:
    images/photo-01.jpg through images/photo-13.jpg

    Type your captions below in this format (copy-paste friendly):
    photo-01 caption: [your text]
    photo-01 alt: [your text]
    photo-02 caption: [your text]
    ...and so on through photo-13.
  </how-to-verify>
  <files>data.json</files>
  <action>Human provides captions and alt text for all 13 photos by viewing images/photo-01.jpg through images/photo-13.jpg and supplying one sentence of story context per photo.</action>
  <verify>
    <automated>MISSING — captions provided by human in this checkpoint; automated check runs in Task 2 via grep -c "PLACEHOLDER" data.json</automated>
  </verify>
  <done>All 13 captions and alt texts provided; no PLACEHOLDER strings remain in photos array of data.json after Task 2 applies them.</done>
  <resume-signal>Paste all 13 captions and alt texts, then type "captions ready"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update data.json captions + build photos.html + add gallery CSS to style.css</name>
  <files>data.json, photos.html, style.css</files>
  <action>
    **Step A — Update data.json:**
    Replace all 13 PLACEHOLDER caption and alt values with the content provided in Task 1.
    Do NOT modify any other field. Verify with: `grep -c "PLACEHOLDER" data.json` — the count
    for photos entries must be 0. (Other sections like knight_lab still have PLACEHOLDER — that
    is correct and should not be changed.)

    **Step B — Add gallery CSS to style.css:**
    Append the following CSS block at the bottom of style.css, after the existing press-desc rule.
    Use exactly these class names — the HTML in Step C depends on them.

    ```css
    /* Photography page — Phase 4 */
    .photo-page {
      max-width: var(--max-width-wide);
      margin: var(--space-12) auto;
      padding: 0 var(--space-8);
    }

    .photo-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: var(--space-4);
      margin-top: var(--space-8);
    }

    .photo-thumb {
      aspect-ratio: 3 / 2;
      overflow: hidden;
      border-radius: var(--border-radius);
      cursor: pointer;
      background: var(--color-bg-subtle);
      border: none;
      padding: 0;
    }

    .photo-thumb img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
      transition: transform var(--transition-fast);
    }

    .photo-thumb:hover img,
    .photo-thumb:focus img {
      transform: scale(1.03);
    }

    .photo-lightbox {
      border: none;
      background: transparent;
      max-width: 90vw;
      max-height: 90vh;
      padding: 0;
    }

    .photo-lightbox::backdrop {
      background: rgba(0, 0, 0, 0.88);
    }

    .lightbox-figure {
      position: relative;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .lightbox-figure img {
      max-width: 85vw;
      max-height: 80vh;
      object-fit: contain;
      display: block;
      border-radius: var(--border-radius);
    }

    .lightbox-caption {
      color: #ffffff;
      font-family: var(--font-ui);
      font-size: var(--text-sm);
      text-align: center;
      margin-top: var(--space-3);
      max-width: 600px;
    }

    .lightbox-close,
    .lightbox-prev,
    .lightbox-next {
      position: fixed;
      background: rgba(255, 255, 255, 0.15);
      border: none;
      color: #ffffff;
      font-size: var(--text-xl);
      cursor: pointer;
      padding: var(--space-3) var(--space-4);
      border-radius: var(--border-radius);
      transition: background var(--transition-fast);
    }

    .lightbox-close { top: var(--space-4); right: var(--space-4); }
    .lightbox-prev  { top: 50%; left: var(--space-4);  transform: translateY(-50%); }
    .lightbox-next  { top: 50%; right: var(--space-4); transform: translateY(-50%); }

    .lightbox-close:hover,
    .lightbox-prev:hover,
    .lightbox-next:hover {
      background: rgba(255, 255, 255, 0.3);
    }
    ```

    **Step C — Replace photos.html entirely:**
    The current stub has `class="page-content"` on `<main>` — this must become `class="photo-page"`.
    The `<dialog>` must be placed OUTSIDE `<main>` (it is a modal overlay, not page content).
    Use `<button class="photo-thumb">` (not `<img>` directly) so the element is keyboard-focusable.
    Use `loading="lazy"` on every thumbnail `<img>`.
    Use `fetch('./data.json')` (with the `./` prefix — required for GitHub Pages root URL compatibility).
    Attach the keydown listener to the `dialog` element, not `window` or `document`.
    Touch swipe uses a 50px threshold: only trigger navigate if `Math.abs(dx) > 50`.

    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Photography — Grace Gormley</title>
      <link rel="preconnect" href="https://fonts.googleapis.com">
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
      <link href="https://fonts.googleapis.com/css2?family=Newsreader:ital,opsz,wght@0,6..72,300..800;1,6..72,300..800&family=Inter:opsz,wght@14..32,400;14..32,500;14..32,600&display=swap" rel="stylesheet">
      <link rel="stylesheet" href="style.css">
      <script type="module" src="components.js"></script>
    </head>
    <body>
      <site-nav></site-nav>
      <main class="photo-page">
        <h1>Photography</h1>
        <div id="photo-grid" class="photo-grid"></div>
      </main>
      <dialog id="lightbox" class="photo-lightbox" aria-label="Photo lightbox">
        <button class="lightbox-close" id="lb-close" aria-label="Close">&times;</button>
        <button class="lightbox-prev" id="lb-prev" aria-label="Previous photo">&#8592;</button>
        <button class="lightbox-next" id="lb-next" aria-label="Next photo">&#8594;</button>
        <figure class="lightbox-figure">
          <img id="lb-img" src="" alt="">
          <figcaption id="lb-caption" class="lightbox-caption"></figcaption>
        </figure>
      </dialog>
      <site-footer></site-footer>
      <script>
        let photos = [];
        let current = 0;

        const dialog = document.getElementById('lightbox');
        const lbImg  = document.getElementById('lb-img');
        const lbCap  = document.getElementById('lb-caption');

        function openLightbox(index) {
          current = index;
          const p = photos[current];
          lbImg.src = p.file;
          lbImg.alt = p.alt;
          lbCap.textContent = p.caption;
          dialog.showModal();
        }

        function navigate(dir) {
          current = (current + dir + photos.length) % photos.length;
          const p = photos[current];
          lbImg.src = p.file;
          lbImg.alt = p.alt;
          lbCap.textContent = p.caption;
        }

        dialog.addEventListener('keydown', e => {
          if (e.key === 'ArrowRight') navigate(1);
          if (e.key === 'ArrowLeft')  navigate(-1);
          if (e.key === 'Escape')     dialog.close();
        });

        dialog.addEventListener('click', e => {
          if (e.target === dialog) dialog.close();
        });

        document.getElementById('lb-prev').addEventListener('click', () => navigate(-1));
        document.getElementById('lb-next').addEventListener('click', () => navigate(1));
        document.getElementById('lb-close').addEventListener('click', () => dialog.close());

        let touchStartX = 0;
        dialog.addEventListener('touchstart', e => {
          touchStartX = e.changedTouches[0].clientX;
        }, { passive: true });
        dialog.addEventListener('touchend', e => {
          const dx = e.changedTouches[0].clientX - touchStartX;
          if (Math.abs(dx) > 50) navigate(dx < 0 ? 1 : -1);
        });

        fetch('./data.json')
          .then(r => r.json())
          .then(data => {
            photos = data.photos || [];
            const grid = document.getElementById('photo-grid');
            if (photos.length === 0) {
              grid.innerHTML = '<p>No photos available.</p>';
              return;
            }
            grid.innerHTML = photos.map((p, i) => `
              <button class="photo-thumb" data-index="${i}" aria-label="View photo: ${p.alt}">
                <img src="${p.file}" alt="${p.alt}" loading="lazy">
              </button>
            `).join('');
            grid.querySelectorAll('.photo-thumb').forEach(btn => {
              btn.addEventListener('click', () => openLightbox(+btn.dataset.index));
            });
          })
          .catch(() => {
            document.getElementById('photo-grid').innerHTML =
              '<p>Unable to load photos. Please try again.</p>';
          });
      </script>
    </body>
    </html>
    ```

    Do NOT add a separate photos.js file — keep JS inline per the established clips.html pattern.
    Do NOT add thumbnail-specific image variants — the same photo-NN.jpg file serves both grid and lightbox.
  </action>
  <verify>
    <automated>grep -c "PLACEHOLDER" /Users/gracegormley/Desktop/Grace\'s\ Website/data.json</automated>

    Expected: the photos section contributes 0 PLACEHOLDER strings.
    (knight_lab and videos sections still have PLACEHOLDER — confirm those are unchanged.)

    Also confirm photos.html structure:
    grep -c "photo-grid" /Users/gracegormley/Desktop/Grace\'s\ Website/photos.html
    grep -c "showModal" /Users/gracegormley/Desktop/Grace\'s\ Website/photos.html
    grep -c "photo-page" /Users/gracegormley/Desktop/Grace\'s\ Website/photos.html
    grep -c "touchstart" /Users/gracegormley/Desktop/Grace\'s\ Website/photos.html

    All four should return 1 or more.

    Also confirm CSS added to style.css:
    grep -c "photo-grid" /Users/gracegormley/Desktop/Grace\'s\ Website/style.css

    Should return 1.
  </verify>
  <done>
    - data.json: all 13 photos[].caption and photos[].alt contain real story context (zero PLACEHOLDER)
    - photos.html: complete page with <main class="photo-page">, #photo-grid div, <dialog id="lightbox"> outside <main>, inline <script> with fetch + lightbox logic
    - style.css: .photo-page, .photo-grid, .photo-thumb, .photo-lightbox, and all lightbox control classes appended at bottom
  </done>
</task>

</tasks>

<verification>
Before marking this plan complete:
1. `grep -c "PLACEHOLDER" data.json` — count must be exactly the non-photo PLACEHOLDERs (knight_lab description + 2 videos.embedUrl = 3). Photos section: 0.
2. `grep "photo-grid\|showModal\|photo-page\|touchstart" photos.html` — all four patterns present.
3. `grep "photo-grid" style.css` — present.
4. Open photos.html via `python3 -m http.server` in the project root, visit http://localhost:8000/photos.html — grid of 13 thumbnails should appear. Click one — lightbox opens with caption.
</verification>

<success_criteria>
- All 13 photo captions and alt texts are real, journalistic, non-placeholder content in data.json
- photos.html displays a 3-column (desktop) to 1-column (mobile) CSS Grid of 13 thumbnails
- Clicking any thumbnail opens the <dialog> lightbox with full-size image and caption
- Arrow keys navigate between photos in the lightbox; Escape closes it
- Touch swipe left/right navigates the lightbox
- Gallery container uses --max-width-wide (1100px), not --max-width-content (720px)
- No PLACEHOLDER strings remain in the photos array of data.json
</success_criteria>

<output>
After completion, create `.planning/phases/04-photography-gallery/04-01-SUMMARY.md`
</output>
