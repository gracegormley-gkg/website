---
phase: 05-featured-projects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - style.css
  - knight-lab.html
autonomous: true
requirements:
  - KNIGHT-01
  - KNIGHT-02
  - KNIGHT-03
  - KNIGHT-04
  - KNIGHT-05

must_haves:
  truths:
    - "The Knight Lab page explains what the EIS Archives project is and why it matters in under 2 minutes of reading"
    - "Grace's role as AI Team Lead who built the metadata pipeline is explicitly stated in prose"
    - "All 7 tools render as visible tag pills: BERTopic, OCR, Topic Modelling, Local LLMs, GPT-4, Claude Code, Supercompute Cluster"
    - "The prose explicitly says Grace led a non-technical team and translated complex AI/data concepts"
    - "A prominent btn-download CTA links to https://nulib-ds.github.io/EIS-Final/"
  artifacts:
    - path: "style.css"
      provides: "Phase 5 CSS — tool-tags, tool-tag, pillars-list, pillar-card, pillar-name classes"
      contains: "Knight Lab and SUSO Camp — Phase 5"
    - path: "knight-lab.html"
      provides: "Full Knight Lab feature page"
      contains: "EIS Archives"
  key_links:
    - from: "knight-lab.html"
      to: "style.css"
      via: "class references: tool-tags, tool-tag, section-heading, btn-download, about-tagline"
      pattern: "class=\"tool-tag\""
    - from: "knight-lab.html"
      to: "https://nulib-ds.github.io/EIS-Final/"
      via: "btn-download anchor href"
      pattern: "nulib-ds\\.github\\.io/EIS-Final"
---

<objective>
Add Phase 5 CSS to the design system and fill knight-lab.html with full editorial content covering the EIS Archives project.

Purpose: The Knight Lab page is one of Grace's strongest differentiators — AI team lead on a digital humanities project at Northwestern. Editors and data journalism recruiters need to understand her specific technical contribution at a glance.
Output: style.css with Phase 5 classes; knight-lab.html fully authored replacing the "Content coming soon" stub.
</objective>

<execution_context>
@/Users/gracegormley/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gracegormley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gracegormley/Desktop/Grace's Website/.planning/PROJECT.md
@/Users/gracegormley/Desktop/Grace's Website/.planning/ROADMAP.md
@/Users/gracegormley/Desktop/Grace's Website/.planning/phases/05-featured-projects/05-RESEARCH.md

<interfaces>
<!-- Existing CSS classes the executor can use directly — no exploration needed. -->

From style.css (already present, do NOT redefine):
  .page-content     — max-width 720px centered column, margin auto, padding 0 var(--space-8)
  .section-heading  — font-size var(--text-xl), border-bottom 2px solid var(--color-border), margin-top var(--space-8)
  .about-tagline    — font-family var(--font-editorial), font-size var(--text-lg), color var(--color-ink-muted), font-style italic
  .btn-download     — inline-block, background var(--color-accent) navy, color #ffffff, padding var(--space-3) var(--space-6), border-radius 4px
  .press-item       — padding var(--space-6) 0, border-bottom 1px solid var(--color-border)
  .press-outlet     — font-ui, text-xs, uppercase, letter-spacing 0.05em, ink-muted
  .press-title      — font-size var(--text-lg), with anchor styles
  .press-desc       — font-size var(--text-sm), color var(--color-ink-muted)

CSS tokens available:
  --font-ui, --font-editorial
  --text-xs (.75rem), --text-sm (.875rem), --text-base (1rem), --text-lg (1.125rem), --text-xl (1.5rem)
  --color-ink (#1a1a1a), --color-ink-muted (#555), --color-border (#e0e0e0)
  --color-bg (#fff), --color-bg-subtle (#f9f8f6), --color-accent (#1a3a5c)
  --space-1 (4px) through --space-24 (96px)
  --border-radius (4px)

New CSS classes to ADD (do not already exist):
  .tool-tags, .tool-tag, .pillars-list, .pillar-card, .pillar-name

From data.json — knight_lab object (use these exact values):
  project_name: "EIS Archives"
  subtitle: "World's Largest Exploratory EIS Database"
  live_url: "https://nulib-ds.github.io/EIS-Final/"
  grace_role: "AI Team Lead — Metadata Pipeline"
  tools: ["BERTopic", "OCR", "topic modelling", "local LLMs", "GPT-4", "Claude Code", "supercompute cluster"]

Existing stub knight-lab.html structure (to be fully replaced):
  Head is already correct: charset, viewport, title "Knight Lab — Grace Gormley", Google Fonts links, style.css, components.js
  Body has: <site-nav>, <main class="page-content">, <site-footer>
  Replace only the content inside <main> — preserve all head tags and web component tags exactly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 5 CSS to style.css</name>
  <files>/Users/gracegormley/Desktop/Grace's Website/style.css</files>
  <action>
Append a new Phase 5 block to the END of style.css. Do NOT modify any existing rules. Add exactly:

```css
/* Knight Lab and SUSO Camp — Phase 5 */
.tool-tags {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-2);
  margin: var(--space-4) 0;
}

.tool-tag {
  font-family: var(--font-ui);
  font-size: var(--text-sm);
  padding: var(--space-1) var(--space-3);
  background: var(--color-bg-subtle);
  border: 1px solid var(--color-border);
  border-radius: var(--border-radius);
  color: var(--color-ink);
}

.pillars-list {
  margin: var(--space-8) 0;
  display: grid;
  gap: var(--space-6);
}

.pillar-card {
  padding: var(--space-6);
  background: var(--color-bg-subtle);
  border-left: 3px solid var(--color-accent);
  border-radius: var(--border-radius);
}

.pillar-name {
  font-family: var(--font-editorial);
  font-size: var(--text-lg);
  font-weight: 600;
  margin-bottom: var(--space-2);
}
```

No inline styles anywhere. No overriding existing tokens.
  </action>
  <verify>
    <automated>grep -c "tool-tags\|tool-tag\|pillars-list\|pillar-card\|pillar-name" "/Users/gracegormley/Desktop/Grace's Website/style.css"</automated>
  </verify>
  <done>style.css ends with the Phase 5 block; grep finds all 5 class names; no existing rules were modified.</done>
</task>

<task type="auto">
  <name>Task 2: Write knight-lab.html full editorial content</name>
  <files>/Users/gracegormley/Desktop/Grace's Website/knight-lab.html</files>
  <action>
Replace the entire contents of knight-lab.html. The head block and web component tags are already correct — replicate them exactly, only replacing the content inside `<main class="page-content">`.

The finished file must contain these sections in order:

1. h1: "Knight Lab" with class="page-content" on main (already set on stub)
2. Subtitle: `<p class="about-tagline">EIS Archives — World's Largest Exploratory EIS Database</p>`
3. h2 "About the Project" (class="section-heading"): 2–3 sentences explaining the EIS Archives. Use this prose (do not deviate): "The EIS Archives is a digital humanities project at Northwestern University's Knight Lab that makes 115 Environmental Impact Statements searchable and explorable for the first time. The collection spans 10 major themes and 17 subthemes, organized around climate change narratives drawn from federal environmental documents. Visitors can explore an interactive theme network, a geographic map of all documents, and curated thematic exhibitions."
4. h2 "Grace's Contribution" (class="section-heading"): Must include the phrase "AI Team Lead" in bold (`<strong>`), mention the metadata pipeline, AND explicitly say she led a non-technical team and translated AI/data science concepts for collaborators. Use this prose (do not deviate): "Grace served as <strong>AI Team Lead</strong>, building the metadata pipeline that powers the theme network visualization and the geographic document map. She led a non-technical team through the technical dimensions of the project — translating AI and data science concepts into actionable workflows for collaborators without a computer science background."
5. h2 "Tools &amp; Skills" (class="section-heading"): Followed by a `<div class="tool-tags">` containing 7 `<span class="tool-tag">` elements, one per tool: BERTopic, OCR, Topic Modelling, Local LLMs, GPT-4, Claude Code, Supercompute Cluster. (Capitalize "Topic Modelling" and "Supercompute Cluster" for readability.)
6. h2 "Explore the Live Project" (class="section-heading"): A brief sentence "The EIS Archives is live and publicly accessible:" followed by the CTA link: `<a href="https://nulib-ds.github.io/EIS-Final/" target="_blank" rel="noopener" class="btn-download">Visit the EIS Archives</a>`

Critical rules:
- Only one `<h1>` per page (the page title "Knight Lab")
- All external links must have `target="_blank" rel="noopener"`
- No `<style>` blocks inside the HTML file — all CSS is in style.css
- Do NOT change the `<head>` block or web component tags
- The `<main>` tag must keep `class="page-content"` exactly as it is on the stub
  </action>
  <verify>
    <automated>grep -c "tool-tag\|btn-download\|nulib-ds\.github\.io\|non-technical team\|AI Team Lead\|section-heading" "/Users/gracegormley/Desktop/Grace's Website/knight-lab.html"</automated>
  </verify>
  <done>knight-lab.html contains all 7 tool tags, the btn-download CTA linking to the EIS Archives, "AI Team Lead" in prose, "non-technical team" in prose, and at least 3 section-heading h2s. The page opens in a browser without errors and nav/footer render.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Open `/Users/gracegormley/Desktop/Grace's Website/knight-lab.html` in a browser (file://) — confirm nav, page title, all 4 section headings, 7 tool tags, and CTA button render.
2. Confirm "AI Team Lead" and "non-technical team" appear in the Grace's Contribution section.
3. Confirm CTA button href is exactly `https://nulib-ds.github.io/EIS-Final/`.
4. Confirm no `<style>` blocks inside knight-lab.html.
5. `grep "Knight Lab and SUSO Camp" style.css` returns a match.
</verification>

<success_criteria>
- style.css has Phase 5 block with all 5 new classes (tool-tags, tool-tag, pillars-list, pillar-card, pillar-name)
- knight-lab.html has all 4 sections with correct prose, all 7 tool tags, btn-download CTA
- KNIGHT-01: Project context readable in under 2 minutes — confirmed
- KNIGHT-02: "AI Team Lead" and metadata pipeline explicitly stated — confirmed
- KNIGHT-03: All 7 tools visible as tag pills — confirmed
- KNIGHT-04: "non-technical team" and concept translation mentioned — confirmed
- KNIGHT-05: btn-download links to https://nulib-ds.github.io/EIS-Final/ — confirmed
</success_criteria>

<output>
After completion, create `/Users/gracegormley/Desktop/Grace's Website/.planning/phases/05-featured-projects/05-01-SUMMARY.md`
</output>
